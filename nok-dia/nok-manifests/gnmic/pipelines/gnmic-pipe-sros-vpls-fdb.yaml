apiVersion: operator.gnmic.dev/v1alpha1
kind: Pipeline
metadata:
  namespace: nok-dia
  name: sros-vpls-fdb
spec:
  clusterRef: dia-core-metrics
  enabled: true
  targetSelectors:
    - matchLabels:
        type: core
  subscriptionSelectors:
    - matchLabels:
        type: vpls-fdb
  outputs:
    outputSelectors:
      - matchLabels:
          type: dia-ext-output
